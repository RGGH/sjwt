# SurrealDB JWT demo

## start SurrealDB

    `surreal start --log debug --auth -u root -p root --deny-guests --no-banner`

### generate a token

    `cargo r`

### use token in test_jwt.py

    `python test_jwt.py`
    
